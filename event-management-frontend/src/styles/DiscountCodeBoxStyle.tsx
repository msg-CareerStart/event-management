import { makeStyles, Theme } from '@material-ui/core';

const useDiscountBoxStyles = makeStyles((theme) => ({
  button: {
    marginTop: '2.3%',
    marginLeft: '12%',
  },
}));

export { useDiscountBoxStyles };
